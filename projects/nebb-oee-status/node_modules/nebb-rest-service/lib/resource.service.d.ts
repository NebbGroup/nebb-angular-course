import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Serializer } from './serializer';
import { QueryOptions } from './query-options';
import { Resource } from './resource.model';
export declare class ResourceService<T extends Resource> {
    private httpClient;
    private url;
    private endpoint;
    private serializer;
    constructor(httpClient: HttpClient, url: string, endpoint: string, serializer: Serializer);
    create(item: T): Observable<T>;
    update(item: T): Observable<T>;
    get(id: number): Observable<T>;
    list(queryOptions: QueryOptions): Observable<T[]>;
    delete(id: number): Observable<Object | T>;
    private handleError(operation?, result?);
    private log(message);
}
